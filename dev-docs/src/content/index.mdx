# DevWorld Dev Journal 

1. Shadcn UI Styling 
2. 마이크로 프론트엔드에서 CSS 격리 문제
3. Auth 앱 라우팅
4. TailwindCSS Purge CSS 클래스
5. RsPack 무한 리컴파일
6. Auth Redirect
7. Nextra Setup
8. Biome 설정하기
9. Biome Nestjs decorator 설정해주기 
10. Docker Compose Up으로 실행되던 nest와 Postgresql 환경을 postgresql은 로컬에 설치한다 우선
11. 기존 FSD 설계를 내 프로젝트에 맞게 어떻게 바꿀것인가?? 각 레이어에 대해서 pages, widgets은 UI의 성격이 강하도록, features와 entites는 비즈니스 로직과 API 응답의 타입이나. zod resolver schema와 같은 책임으로 나눈다. 
12. Toaster 프로바이더를 각 마이크로 앱에 둬야하는가? 쉘에 둬야 하는가? 근데 ui에 모듈로 뺴고 각각 shared로 import 한다면 자바스크립트 번들러는 하나의 싱글톤 객체로 관리해준다. 
13. signin form에서 비즈니스 로직을 기준으로 재사용을 케이스를 추상화할것인가. UI를 기준으로 추상화 할 것인가? -> UI는 유연하게 열고 각각의 비즈니스 로직, 이경우에는 OAUTH였다.를 받아주는게 낫다라는 것을 알았음.
14. oauth provider type에 따른 추상화와 서비스 로직 분리.
15. react frontend에서 oauth dynamic routes 생성, 커스텀 훅 분리하기
16. rspack에서 png 모듈 어떻게 잡아야 하나?
17. text components 추상화 시킬 것인가?