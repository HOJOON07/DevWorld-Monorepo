const isDev = typeof process !== 'undefined' && process.env?.NODE_ENV === 'development';

// export const BASE_URL = isDev ? 'http://localhost:5500' : 'https://api.devworld.com';
export const BASE_URL = 'http://localhost:5500';
